import { Router } from "express";
import { MagicService } from '../services';

export const MagicRouter = (router: Router, magicSvc: MagicService): void => {
    // const prefix = `[ThemeRouter]`;
    
};
